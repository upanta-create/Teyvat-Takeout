apiVersion: v1
kind: Service
metadata:
  name: teyvat-admin
  namespace: teyvat-takeout
  labels:
    app: teyvat-admin
    tier: admin
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: teyvat-admin
