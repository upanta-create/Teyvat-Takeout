==============================================================================
🍜 Teyvat Takeout Helm Chart has been successfully deployed!
==============================================================================

Release Name: {{ .Release.Name }}
Namespace:    {{ .Release.Namespace }}
Environment:  {{ .Values.global.environment }}

1. Check deployment status of all pods:
   $ kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. View deployed services:
   $ kubectl get svc -n {{ .Release.Namespace }}

{{- if .Values.ingress.enabled }}
3. Ingress endpoints:
{{- range .Values.ingress.hosts }}
   - Frontend Portal:  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/
   - Admin Dashboard:  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/admin
   - REST API:         http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/api
   - Swagger OpenAPI:  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}/api/docs
{{- end }}
{{- else }}
3. Port-forward to access locally without Ingress:
   $ kubectl port-forward svc/{{ include "teyvat-takeout.fullname" . }}-frontend 8080:80 -n {{ .Release.Namespace }}
   $ kubectl port-forward svc/{{ include "teyvat-takeout.fullname" . }}-admin 8081:80 -n {{ .Release.Namespace }}
   $ kubectl port-forward svc/{{ include "teyvat-takeout.fullname" . }}-backend 4000:4000 -n {{ .Release.Namespace }}
{{- end }}

{{- if .Values.backend.autoscaling.enabled }}
4. Check Horizontal Pod Autoscaler (HPA):
   $ kubectl get hpa -n {{ .Release.Namespace }}
{{- end }}

{{- if .Values.serviceMonitor.enabled }}
5. Prometheus metrics scraping:
   - Scrape endpoint: /metrics on port {{ .Values.backend.service.port }}
   - ServiceMonitor:  {{ include "teyvat-takeout.fullname" . }}-backend-monitor
{{- end }}

==============================================================================
